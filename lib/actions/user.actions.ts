'use server'

export const signIn = async() => {
    try{

    } catch(error){
        console.log('Error',error);
    }
}

export const signUp = async(userDate: SignUpParams) => {
    try{

    } catch(error){
        console.log('Error',error);
    }
}